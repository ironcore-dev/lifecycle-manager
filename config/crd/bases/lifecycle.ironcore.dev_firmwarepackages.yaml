---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: firmwarepackages.lifecycle.ironcore.dev
spec:
  group: lifecycle.ironcore.dev
  names:
    kind: FirmwarePackage
    listKind: FirmwarePackageList
    plural: firmwarepackages
    shortNames:
    - fi
    singular: firmwarepackage
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Firmware package name
      jsonPath: .spec.name
      name: Package Name
      type: string
    - description: Firmware package version
      jsonPath: .spec.version
      name: Version
      type: string
    - description: Reflects whether reboot required
      jsonPath: .spec.rebootRequired
      name: Reboot required
      type: boolean
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FirmwarePackage is the Schema for the firmwarepackages API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: FirmwarePackageSpec defines the desired state of FirmwarePackage.
            properties:
              machineTypeRef:
                description: MachineTypeRef contain reference to MachineType object.
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              name:
                description: Name contains the name of the package.
                type: string
              rebootRequired:
                description: RebootRequired reflects whether the machine reboot required
                  during package installation.
                type: boolean
              source:
                description: Source contains the URL where the package can be downloaded
                  from.
                type: string
              version:
                description: Version contains the version of the package.
                type: string
            required:
            - machineTypeRef
            - name
            - rebootRequired
            - version
            type: object
          status:
            description: FirmwarePackageStatus defines the observed state of FirmwarePackage.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
