---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: machines.lifecycle.ironcore.dev
spec:
  group: lifecycle.ironcore.dev
  names:
    kind: Machine
    listKind: MachineList
    plural: machines
    singular: machine
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Machine is the Schema for the machines API.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: MachineSpec defines the desired state of Machine.
            properties:
              machineTypeRef:
                description: MachineTypeRef contain reference to MachineType object.
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              oobMachineRef:
                description: OOBMachineRef contains reference to OOB machine object.
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              packages:
                description: Packages defines the list of package versions to install.
                items:
                  description: PackageVersion defines the concrete package version
                    item.
                  properties:
                    name:
                      description: Name defines the name of the firmware package.
                      type: string
                    version:
                      description: Version defines the version of the firmware package.
                      type: string
                  required:
                  - name
                  - version
                  type: object
                type: array
              scanPeriod:
                description: ScanPeriod defines the interval between scans.
                type: string
            required:
            - machineTypeRef
            - oobMachineRef
            - scanPeriod
            type: object
          status:
            description: MachineStatus defines the observed state of Machine.
            properties:
              installedPackages:
                description: InstalledPackages reflects the versions of installed
                  firmware packages.
                items:
                  description: PackageVersion defines the concrete package version
                    item.
                  properties:
                    name:
                      description: Name defines the name of the firmware package.
                      type: string
                    version:
                      description: Version defines the version of the firmware package.
                      type: string
                  required:
                  - name
                  - version
                  type: object
                type: array
              lastScanResult:
                description: LastScanResult reflects either success or failure of
                  the last scan job.
                type: string
              lastScanTime:
                description: LastScanTime reflects the timestamp when the last scan
                  job for installed firmware versions was performed.
                format: date-time
                type: string
              message:
                description: Message contains verbose message explaining current state
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
