---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: machines.lifecycle.ironcore.dev
spec:
  group: lifecycle.ironcore.dev
  names:
    kind: Machine
    listKind: MachineList
    plural: machines
    shortNames:
    - mc
    singular: machine
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Last scan timestamp
      jsonPath: .status.lastScanTime
      name: Last Scan Time
      type: string
    - description: Last scan result
      jsonPath: .status.lastScanResult
      name: Last Scan Result
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Machine is the schema for Machine API object.
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: MachineSpec contains desired configuration of machine lifecycle.
            properties:
              machineTypeRef:
                description: MachineTypeRef contain reference to MachineType object.
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              oobMachineRef:
                description: OOBMachineRef contains reference to OOB machine object.
                properties:
                  name:
                    description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Add other useful fields. apiVersion, kind, uid?'
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              scanPeriod:
                description: ScanPeriod defines the interval between scans.
                type: string
            required:
            - machineTypeRef
            - oobMachineRef
            - scanPeriod
            type: object
          status:
            description: MachineStatus contains observed state of Machine object.
            properties:
              installedPackages:
                description: InstalledPackages contains the list of references to
                  FirmwarePackage objects.
                items:
                  description: LocalObjectReference contains enough information to
                    let you locate the referenced object inside the same namespace.
                  properties:
                    name:
                      description: 'Name of the referent. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        TODO: Add other useful fields. apiVersion, kind, uid?'
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              lastScanResult:
                description: LastScanResult reflects the state of the last scan.
                enum:
                - Success
                - Failure
                type: string
              lastScanTime:
                description: LastScanTime reflects the timestamp when the last scan
                  of available packages was done.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
